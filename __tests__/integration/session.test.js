// const request = require("supertest");

// const app = require("../../src/app");

// const { User } = require("../../src/app/models");

describe("Auth", () => {
	it("teste jest", async () => {
		const x = 2 + 2;

		expect(x).toBe(4);
	});
});
